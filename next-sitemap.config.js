/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: "https://www.vasumathasoftech.com", // Change this to your actual domain
    generateRobotsTxt: true, // Generates robots.txt
    sitemapSize: 5000,
    //exclude: ["/admin", "/dashboard"], // Exclude private pages
  };
  